<div class= "message <%= message.user_id == current_user.id ? 'me' : 'other'%>">
    <div class= "content">
      <%= message.content %>
      <%= message.created_at.strftime('%Y年/%m月%d日/%k:%M')%>
    </div>
</div>

